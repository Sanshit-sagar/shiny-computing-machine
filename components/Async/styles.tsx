import { styled } from 'stitches.config'

export const StyledRoot = styled('div', {
    
})

export const StyledAwaiting = styled('span', {

})

export const StyledResolved = styled('span', {

})

export const StyledRejected = styled('span', {

})

export const StyledIdle = styled('span', {

})